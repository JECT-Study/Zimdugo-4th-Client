/**
 * 로그인해야 볼 수 있는 경로. 로케일 접두사를 벗긴 형태로 적는다.
 *
 * 보호 라우트를 추가할 때 **여기 한 곳만** 고치면 세 가지가 따라온다.
 *
 * 1. `server-protected-route-guard` 가 비로그인 문서 요청을 서버에서 막는다
 * 2. `vite.config.ts` 가 프리렌더 대상에서 뺀다 — 빼지 않으면 빌드 때 비로그인으로
 *    판정돼 홈으로 리다이렉트된 결과가 그 경로의 정적 파일로 굳고, 그 파일이 서빙되면
 *    서버 가드까지 건너뛴다
 * 3. `useRedirectWhenUnauthenticated` 가 체류 중 인증이 끊기면 내보낸다
 *
 * 목록이 세 곳에 흩어져 있으면 하나를 빠뜨렸을 때 증상이 라우트 파일과 동떨어진
 * 곳에서 나타나 찾기 어렵다. 그래서 이 파일이 유일한 출처다.
 *
 * ⚠️ 이 목록에 넣는다고 서버가 **인가**를 해주는 것은 아니다. 서버 판정이 읽는
 * `auth-storage` 는 클라이언트가 쓰는 쿠키라 위조할 수 있다. 라우팅 UX 장치이지
 * 접근 통제가 아니며, 실제 통제는 액세스 토큰을 요구하는 API 가 한다.
 * 보호 경로에서 SSR 로 사용자 데이터를 다루려면(로더·프리페치·메타 태그) 서버가
 * 신원을 확인할 수단을 먼저 만들어야 한다.
 *
 * 별칭 import 를 쓰지 않는다. `vite.config.ts` 가 상대 경로로 그대로 읽어 간다.
 */
export const PROTECTED_PATHS = [
  "/report",
  "/my/reports",
  "/my/favorites",
] as const;
